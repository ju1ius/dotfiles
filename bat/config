# vim:ft=conf
# This is `bat`s configuration file.
# Each line either contains a comment or a command-line option
# that you want to pass to `bat` by default.
#
# Run `bat --help` to get a list of all possible configuration options.

# see:`bat --list-themes`
--theme=TwoDark
